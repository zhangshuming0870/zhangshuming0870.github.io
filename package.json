{
  "name": "zhangshuming-blog",
  "version": "1.0.0",
  "description": "zhangshuming技术博客",
  "scripts": {
    "build": "gem install bundler && bundle install && bundle exec jekyll build",
    "start": "bundle exec jekyll serve --host 0.0.0.0 --port $PORT || npx serve _site -p $PORT"
  },
  "engines": {
    "ruby": ">= 2.7.0",
    "node": ">= 18.0.0"
  }
}

